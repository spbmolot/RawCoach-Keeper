# RawCoach-Keeper

Starter Laravel 11 application for rawplan.ru with:

- Jetstream with Livewire for authentication and user dashboard
- Filament Admin panel
- Role and permission management via spatie/laravel-permission
- Basic billing models (`Plan`, `UserSubscription`, `Payment`) and placeholders for YooKassa and CloudPayments integrations
- Simple advertising models (`AdPlacement`, `AdCampaign`, `AdCreative`)

## Setup

```bash
composer install
cp .env.example .env
php artisan key:generate
npm install
npm run build
php artisan migrate
```

## Infrastructure Requirements

- PHP 8.2+
- Database (MySQL, PostgreSQL, etc.)
- Redis for cache and queues
- Queue worker (e.g. Supervisor)
- SSL/TLS termination

## Production Deployment

1. Configure `.env` with database, Redis, queue and other production settings.
2. Run migrations: `php artisan migrate --force`.
3. Build assets: `npm run build`.
4. Optimize configuration and routes: `php artisan config:cache` and `php artisan route:cache`.
5. Start queue worker: `php artisan queue:work`.
6. Start scheduler: `php artisan schedule:work` or cron `* * * * * php artisan schedule:run`.

## Вебхуки

Платёжные провайдеры отправляют уведомления на:

- `POST /webhook/yookassa`
- `POST /webhook/cloudpayments`

Контроллеры вебхуков используют внедрение зависимостей и проверяют подписи.


# RawPlan

**RawPlan** — платформа подписок на планы правильного питания для похудения.  
Пользователь регистрируется, выбирает тариф (месяц/год со скидкой или индивидуальный), оплачивает и получает доступ к ежемесячному меню: **завтраки/обеды/ужины на каждый день**, с **точной граммовкой готового блюда**, **калорийностью**, **БЖУ** и **пошаговыми рецептами**. Доступны **PDF/Excel**, генератор **списков покупок**, **личный кабинет** и **админ‑панель**. Есть **платная реклама** и **многоуровневый доступ**.

---

## Основные возможности

- 👤 **Личный кабинет**: «Сегодня», «Неделя», файлы (PDF/Excel), избранное, прогресс, подписка/платежи.
- 📦 **Подписки**: помесячно, год со скидкой, **персональный план** (учёт предпочтений/аллергенов).
- 📅 **Меню по дням**: 3 приёма пищи в день, вес готовой порции, ккал/БЖУ, шаги приготовления/подачи.
- 🛒 **Списки покупок**: на неделю/диапазон дней; экспорт в PDF/Excel; группировка по срокам хранения.
- 🧑‍🍳 **Индивидуальный план**: анкета, фильтры, согласование с нутрициологом, публикация в ЛК.
- 💳 **Оплата и автопродления**: YooKassa / CloudPayments / Тинькофф; купоны/промокоды; вебхуки.
- 🧩 **RBAC**: роли и права (гость, пользователь, подписчики, рекламодатель, редактор, админ).
- 📢 **Реклама**: слоты (header/sidebar/in‑content), CPM/CPC/фикс, модерация, отчёты по показам/кликам.
- 🛠 **Админка**: Filament Admin — меню/рецепты/ингредиенты, месячные файлы, пользователи/платежи, кампании.
- 🔍 **SEO/Analytics**: метатеги, OG, Schema.org (Recipe/Article), sitemap, robots, пиксели аналитики.
- 🔐 **Безопасность**: HTTPS, CSRF/XSS, ограничение логинов, 2FA (опционально), аудит действий.

---

## Технологии

- **Backend**: PHP 8.2+, **Laravel 10/11**
- **DB**: MySQL 8, **Redis** (по возможности — кэш/очереди/сессии)
- **UI**: Blade + **Tailwind CSS** + **Livewire** (админка/ЛК быстры)
- **Admin**: **Filament Admin** / (возможна Laravel Nova)
- **Payments**: YooKassa / CloudPayments (вебхуки, автоплатежи)
- **Deploy**: Timeweb (PHP+MySQL), `cron` для `schedule:run`
- **Mail**: SMTP провайдера (Timeweb или внешний)
- **Monitoring**: Telescope (стейджинг), Sentry/Logtail (прод)

---

## Структура основных страниц

- `/` — **Главная**: УТП, как это работает, примеры, тарифы, отзывы, FAQ, CTA.
- `/tariffs` — **Тарифы/Оплата**: Месяц/Год/Индивидуальный, купоны, чекаут.
- `/login`, `/register`, `/password/reset` — аутентификация.
- `/onboarding` — анкета (цели, аллергены, исключения, предпочтения).
- `/dashboard` — ЛК: «Сегодня», неделя, файлы, подписка/платежи, избранное.
- `/menu/{yyyy-mm}` — каталог месяца: дни 1–31, скачивание PDF/Excel.
- `/recipe/{id}` — рецепт: ингредиенты (нетто), шаги, вес порции, ккал/БЖУ, экспорт/избранное.
- `/shoplist` — генератор списков покупок (диапазон, экспорт).
- `/billing` — управление подпиской, история платежей, счета.
- `/ads` — кабинет рекламодателя: кампании, креативы, оплата, отчёты.
- `/admin` — админ‑панель Filament.

---

## Быстрый старт (локально)

### 1) Требования
- PHP 8.2+, Composer
- MySQL 8.x
- Node 18+ (если используете сборку фронта)
- Redis (желательно)

### 2) Установка
```bash
git clone https://github.com/<org>/<repo>.git rawplan
cd rawplan

composer install
cp .env.example .env
php artisan key:generate
```

Настройте `.env` (см. ниже), затем:
```bash
php artisan migrate --seed    # создаст роли/админа/демо-данные
npm i && npm run build        # если есть фронтовая сборка
php artisan storage:link
php artisan serve
```

### 3) .env (пример)
```dotenv
APP_NAME=RawPlan
APP_ENV=local
APP_KEY=base64:GENERATE_ME
APP_DEBUG=true
APP_URL=http://localhost

LOG_CHANNEL=stack
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=rawplan
DB_USERNAME=root
DB_PASSWORD=secret

CACHE_DRIVER=file
QUEUE_CONNECTION=database
SESSION_DRIVER=file
SESSION_LIFETIME=120

# MAIL
MAIL_MAILER=smtp
MAIL_HOST=smtp.timeweb.ru
MAIL_PORT=587
MAIL_USERNAME=your@mail.ru
MAIL_PASSWORD=yourpass
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=your@mail.ru
MAIL_FROM_NAME="${APP_NAME}"

# Payments
PAYMENT_PROVIDER=yookassa    # yookassa|cloudpayments|tinkoff
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET=
CLOUDPAYMENTS_PUBLIC_ID=
CLOUDPAYMENTS_SECRET=

# Webhooks
WEBHOOKS_SECRET=some-long-secret
```

### 4) Пользователи по умолчанию (через сиды)
- **Админ**: `admin@example.com / password`
- Роли: `admin`, `editor`, `advertiser`, `user`, `subscriber_standard`, `subscriber_premium`, `subscriber_personal`.

(Логины и роли настраиваются в `database/seeders`.)

---

## Архитектура и сущности

**Пользователи/подписки/платежи**
- `User`, `Plan`, `UserSubscription`, `Payment`, `Invoice`, `Coupon`

**Меню и рецепты**
- `Menu` (месяц), `Day` (1..31), `Recipe` (завтрак/обед/ужин),  
  `RecipeIngredient`, `RecipeNutrition`, `DayMeal`

**Индивидуальный план**
- `Questionnaire` (анкета), `PersonalPlan`, `PersonalPlanDay`, `PersonalPlanRecipe`

**Реклама**
- `AdPlacement`, `AdCampaign`, `AdCreative`, `AdStatDaily`

---

## RBAC (роли и права)
Реализовано через пакет `spatie/laravel-permission`.  
Пример: доступ к платному контенту — для ролей `subscriber_*` или при активной записи в `user_subscriptions`.

---

## Оплаты и подписки

- **Поток оплаты:** выбор плана → создание платежа → редирект на страницу провайдера → вебхук подтверждает → запись `payments` → активация `user_subscriptions` (статус `active`, `started_at`, `ends_at`).
- **Автопродление:** за 1 день до `ends_at` инициируется повторное списание (через токен у провайдера); при фейле — `grace` 3 дня, письма‑уведомления.
- **Купоны:** фикс./процентная скидка, срок жизни, лимит.

**Вебхуки:**
```
POST /webhooks/yookassa
POST /webhooks/cloudpayments
```
Проверка подписи (`WEBHOOKS_SECRET`/секрет провайдера), запись в очередь, обновление статусов платежей/подписок.

---

## Списки покупок

- Генерация на **неделю** или произвольные дни, суммирование в **г/мл/шт** (нормализация единиц), экспорт **PDF/Excel**.
- Группировка: «купить на месяц», «раз в 1–2 недели», «еженедельно».
- Интеграция с ЛК: чекбоксы «куплено», сохранение вариантов.

---

## Загрузка файлов меню (PDF/Excel)

- Файлы месяца прикрепляются к сущности `Menu` (хранятся в `storage/app/public/menus/<yyyy-mm>/...`).
- Доступ — по активной подписке; ссылки подписанные/временные (Signed URLs).

---

## Админ‑панель (Filament)

- **Контент:** Меню/Дни/Рецепты/Ингредиенты, валидация ккал, веса порции, импорты/экспорты.
- **Файлы:** загрузка месячных PDF/Excel, предпросмотр.
- **Пользователи/Роли/Права**, **Подписки/Платежи/Купоны**.
- **Реклама:** слоты, кампании, креативы, отчёты.
- **Настройки:** бренд, письма, SEO, пиксели, SMTP.
- **Отчёты:** выручка, активные подписки, MRR/Churn, топ‑рецепты.

---

## Тесты
```bash
php artisan test
# или с покрытием
XDEBUG_MODE=coverage php artisan test --coverage-html coverage/
```

---

## Деплой на Timeweb

1. PHP 8.2, Composer в панели.
2. Репозиторий → деплой в `public_html` (или SFTP/Git‑деплой).
3. `composer install --no-dev --optimize-autoloader`
4. Настроить `.env` (DB/Mail/Payments), `php artisan key:generate`
5. `php artisan migrate --force && php artisan storage:link`
6. Крон (в панели):
   ```
   * * * * * php /home/USER/www/artisan schedule:run
   ```
7. Очереди: минимум `database`; при наличии Redis — Supervisor/Horizon (на VPS/Cloud).

---

## Безопасность и соответствие

- HTTPS, CSP, очистка входных данных, CSRF, защита форм логина (rate‑limit/lockout).
- Логи аудита (изменение пароля/почты, биллинг, админ‑действия).
- Политики: публичная оферта, ПДн, Cookie, медицинский дисклеймер.

---

## Roadmap (MVP → развитие)

- **MVP**: регистрация/логин, тарифы/оплата, ЛК (сегодня/неделя/файлы), каталог месяца/рецепт, списки покупок, админка контента, базовый биллинг.
- **Далее**: индивидуальные планы (анкета/чат/правки), блог/SEO, реклама (FIX/CPM/CPC), реферальная программа, PWA, A/B тесты.

---

## Лицензия

Репозиторий приватный.  
**Все права защищены © 2025.**  
Использование кода вне команды запрещено без явного письменного разрешения владельца проекта.



Отлично! Ниже — **максимально подробное** описание функционала проекта *RawPlan* и **ТЗ по страницам**. Я опираюсь на структуру и стандарты ваших меню: три приёма пищи в день, указаны граммовки порций и калорийность, пошаговые инструкции, суточная норма 1200–1400 ккал для комфортного похудения. &#x20;

---

# 1) Цель продукта и ценностное предложение

**Цель:** продавать по подписке готовые планы питания для похудения с подробными рецептами (граммовка **готового блюда**, калорийность, шаги приготовления и подачи), а также предлагать **индивидуальные планы** с учётом предпочтений и аллергенов.

**Что получает клиент:**

* Доступ к **ежемесячному меню** (завтрак/обед/ужин на каждый день месяца) c шагами, формулами приготовления, весом готовых блюд и ккал.&#x20;
* **Скачивание** в PDF/Excel; **недельные списки закупок**; генератор **автоматических списков покупок** по выбранной неделе/дню.
* **Личный кабинет**: план на сегодня/неделю, прогресс, подписка, история платежей, загрузка чеков/счетов, избранное.
* **Индивидуальный план**: анкетирование, фильтры по аллергенам/исключениям, ручная/полуавтоматическая корректировка нутрициологом.
* **Абонементы**: помесячно / год со скидкой; отдельный тариф «Персональный».
* **Платная реклама** на сайте (слоты), без ущерба UX.

---

# 2) Роли и уровни доступа (RBAC)

* **Гость:** просмотр лендинга, тарифов, демо-скриншотов/превью рецептов, регистрация/логин.
* **Пользователь (без подписки):** ЛК (ограниченное превью), оформление подписки, доступ к FAQ/поддержке.
* **Подписчик «Базовый/Стандарт»:** полный доступ к текущему месяцу: все рецепты/калории/граммовки, список покупок, скачивание PDF/Excel.
* **Подписчик «Годовой» (Premium):** как «Стандарт» + ранний доступ к следующему месяцу, архивы прошлых месяцев, дополнительные материалы.
* **Подписчик «Индивидуальный»:** + персональный план и консультации (чат/комментарии к меню).
* **Рекламодатель:** кабинет рекламных кампаний (кампании, креативы, бюджет, платежи, отчёты).
* **Редактор/Нутрициолог:** управление меню/рецептами, проверка анкет, составление индивидуальных планов.
* **Администратор:** все права, биллинг, пользователи, роли, промокоды, объявления, логирование.

(Реализация RBAC: `spatie/laravel-permission`, роли и разрешения на уровне роутов и политик.)

---

# 3) Архитектура и стек

* **Laravel 10/11 (PHP 8.2+)**, монолит с Blade + **Livewire** (быстрые интерфейсы ЛК/админки).
* **MySQL 8** (данные, транзакции), **Redis** (кэш, очереди, сессии) — по возможности.
* **Filament Admin** (админка) / альтернатива: Laravel Nova.
* Платежи: **YooKassa**/**CloudPayments** (разовые/рекуррентные), вебхуки + очередь на обработку.
* Хранилище: локально + S3-совместимое (для PDF/картинок/экспорта).
* Рассылки: Laravel Mail + шаблоны (Blade Mailable).
* Деплой: Timeweb (PHP+MySQL), крон для `schedule:run`.
* Логи и мониторинг: Telescope (стейджинг), Sentry/Logtail (прод).

---

# 4) Схема данных (укороченно)

**Пользователи/подписки/платежи**

* `users`: профиль, настройки, флаги аллергий/исключений.
* `plans`: {name, period(month|year|personal), price, features}.
* `user_subscriptions`: {user\_id, plan\_id, status, started\_at, ends\_at, auto\_renew, provider, external\_id}.
* `payments`: {user\_id, provider, external\_id, amount, currency, status, payload, paid\_at}.
* `invoices`: PDF квитанции.

**Контент меню/рецептов**

* `menus`: {month, year, title, status, pdf\_url, xlsx\_url}.
* `days`: {menu\_id, day\_number, kcal\_total}.
* `recipes`: {title, meal\_type(breakfast|lunch|dinner), steps(md/html), cooking\_method, time\_min, photo}.
* `recipe_nutrition`: {recipe\_id, calories, protein, fat, carbs, serving\_weight\_g}.
* `recipe_ingredients`: {recipe\_id, name, amount\_value, amount\_unit, net\_weight\_g}.
* `day_meals`: {day\_id, recipe\_id, position}.
* `shopping_templates`: правила генерации списков закупок (по неделям).

**Индивидуальные планы**

* `questionnaires`: {user\_id, height, weight, age, sex, goal, activity\_level, allergens\[], dislikes\[], target\_kcal}.
* `personal_plans`: {user\_id, status(draft|review|active), assigned\_nutritionist\_id, comment}.
* `personal_plan_days/recipes`: аналогично обычному меню с персональными подстановками.

**Реклама**

* `ad_placements`: {code, name, size}.
* `ad_campaigns`: {owner\_id, placement\_id, budget, type(CPM|CPC|FIX), start\_at, end\_at, status}.
* `ad_creatives`: {campaign\_id, file/url, target\_url}.
* `ad_stats_daily`: {campaign\_id, date, impressions, clicks, spend}.

---

# 5) Пользовательские сценарии (сквозные)

1. **Регистрация → тариф → оплата → доступ к меню месяца → скачивание PDF/Excel → список покупок на неделю.**
2. **Анкета → выбор «Индивидуальный» → оплата → сбор ограничений (аллергены/исключения) → генерация/ручная правка нутрициологом → публикация персонального плана в ЛК.**
3. **Продление:** авто-списание, уведомления о продлении/счёте, пауза/отмена.

---

# 6) Подписки и биллинг

* **Тарифы**:

    * *Месяц* (30/31 день).
    * *Год* (−15..25% vs месяц).
    * *Индивидуальный* (настройка меню по анкете; выше цена).
* **Оплаты:** хостед-страницы провайдера + вебхуки → подтверждение → активация подписки.
* **Автопродление:** токенизация карты у провайдера, `ends_at`−1 день → попытка списания → обновление статуса.
* **Чеки/акты:** платежная квитанция (PDF), история платежей, письма на e-mail.
* **Купоны/промокоды:** фикс. скидка или %, срок жизни, лимит использований.
* **Грейс-период:** 3 дня после неуспешного списания → мягкая деградация доступа.

---

# 7) Реклама (платная)

* **Слоты**: header, sidebar, внутри статей/рецептов (межблочные), footer.
* **Управление кампаниями** (админ/рекламодатель): бюджет, даты, таргетинг по разделам, загрузка креативов, модерация.
* **Модель:** CPM/CPC/фикс.
* **Счётчик показов/кликов, антифрод**, суточная агрегация `ad_stats_daily`, отчёты.
* **UX-ограничения:** без навязчивых форматов, не перекрывать контент.

---

# 8) Нефункциональные требования

* **Производительность:** TTFB < 500 мс (кэш), LCP < 2.5 c, адаптив (320–1440px).
* **Доступность (A11y):** контраст, alt-тексты, клавиатурная навигация.
* **Безопасность:** HTTPS, CSP, защита от XSS/CSRF, ограничение попыток логина, 2FA (опционально), шифрование PII.
* **SEO:** ЧПУ, метатеги, Open Graph, Schema.org (Recipe/Article), sitemap, robots.
* **Журналы/аудит:** входы, смены пароля/почты, биллинг, админ-операции.
* **Бэкапы:** база/файлы (ежедневно), retention 14–30 дней.
* **Локализация:** RU (с возможностью EN позже).
* **Юр/политики:** Публичная оферта, Политика обработки персональных данных, Cookies, Медицинский дисклеймер.

---

# 9) ТЗ по страницам (подробно)

## 9.1. Главная (лендинг)

**Цель:** конвертировать посетителя в регистрацию/покупку.

**Блоки:**

* **Hero:** УТП + CTA «Попробовать»/«Купить». Превью дня (краткая карточка рецепта с граммовкой и ккал).
* **Как это работает (3 шага):** зарегистрируйся → выбери тариф → получай меню каждый месяц.
* **Что внутри подписки:**

    * Ежедневные завтраки/обеды/ужины с порциями (вес готового блюда) и калорийностью, пошаговые шаги. &#x20;
    * Дневная калорийность 1200–1400 ккал (для похудения).&#x20;
    * Списки покупок (недели), PDF/Excel, экспорт.
* **Тарифы** (кратко) + переключатель «Месяц/Год −%» + «Индивидуальный».
* **Отзывы/доверие:** карточки, соц. доказательства.
* **FAQ**, **CTA** повтор.
* **Рекламные слоты** (неинвазивно).

**Ключевые состояния:** гость/авторизован, подсказки, прелоадеры.
**События/аналитика:** клики по CTA, переходы к тарифам, скролл-глубина, отправка формы.

---

## 9.2. Тарифы/Оплата

**Блоки:**

* Сравнение планов (Месяц / Год / Индивидуальный): цена, фичи, доступ к архивам, поддержка.
* Калькулятор суммы (год — со скидкой).
* Выбор способа оплаты (провайдер), чекбокс оферты, кнопка «Оплатить».
* **После оплаты:** редирект в ЛК; письмо с подтверждением и счётом.

**Ошибки/углы:**

* Отмена на платёжной странице, неуспешное списание, повторная попытка, купон недействителен.

---

## 9.3. Регистрация/Логин/Восстановление

* Email + пароль, политика паролей, подтверждение почты.
* Восстановление (email), смена пароля.
* 2FA (по желанию) — через приложение/почту.
* Согласия на обработку данных, чекбокс рассылок.

---

## 9.4. Онбординг: Анкета (для индивидуального плана и рекомендаций)

* Профиль: пол/возраст/рост/вес/цель/уровень активности.
* **Аллергены** (молочное, глютен, орехи и т.д.), **исключения** (свинина, морепродукты…), релевантные предпочтения.
* Оборудование/время готовки/бюджет.
* Результат: таргетированный **целевой диапазон ккал** и правила персональной фильтрации рецептов.

---

## 9.5. Личный кабинет (Dashboard)

**Верх:** статус подписки, «продлить/изменить», уведомления.

**Вкладки:**

1. **Сегодня**: карточки «Завтрак/Обед/Ужин» на текущий день (вес порции и ккал), чекбокс «готово», быстрый переход к рецепту.
2. **Неделя/Календарь**: навигация по дням месяца, свод по калориям.
3. **Список покупок**: генерация на неделю/диапазон дней, экспорт в PDF/Excel.
4. **Файлы**: PDF меню месяца, Excel-закупки (доступ для активных подписок).
5. **Избранное**: сохранённые рецепты/дни.
6. **Прогресс**: трекинг веса (опционально), заметки.
7. **Подписка и платежи**: текущий план, автопродление, история платежей, счета (PDF).
8. **Индивидуальный план** (если куплен): статус, чат/комментарии от нутрициолога, версии плана.

---

## 9.6. Каталог меню (Месяцы/Архив)

* Сетка месяцев: «Сентябрь 2025» (активный), «Октябрь 2025» (анонс для годового), архив для Premium.
* Внутри месяца: список дней (1–31); у каждого дня — 3 блюда и сводка ккал.
* Кнопки: «Скачать PDF», «Экспорт закупок».
* **Принцип меню:** повтор неделей (15–21 = 1–7; 22–28 = 8–14) — для удобства и стабильности калорийности.&#x20;

---

## 9.7. Страница рецепта

* Заголовок, **вес порции готового блюда**, калории/БЖУ, **способ приготовления** (варка/запекание/тушение…), **время**, инвентарь.&#x20;
* **Ингредиенты** с точной граммовкой (нетто) и возможной заменой.
* **Пошаговые инструкции** (нумерованные, с подсказками по подаче).&#x20;
* Переключатель «на N порций» → автоматический пересчёт.
* Кнопки: «В избранное», «В список покупок», «Скачать в PDF», «Пожаловаться/ошибка».
* Блок «Пищевая ценность» (ккал и БЖУ/порция).
* «Похожие рецепты» (по приёму пищи/калориям/ингредиентам).

---

## 9.8. Список покупок (генератор)

* Диапазон: неделя / произвольные дни.
* Нормализация единиц (г/мл/шт), **суммирование** по неделе и **итог за месяц**.
* Группировка: **на месяц сразу** (крупы/консервы/заморозка), **раз в 1–2 недели** (мясо/рыба — порциями заморозка), **еженедельно** (овощи/фрукты/молочка/зелень).
* Экспорт: PDF/Excel.
* Отмечать «куплено», сохранять листы в ЛК.

---

## 9.9. Управление подпиской

* Переключение планов (апгрейд/даунгрейд), автопродление on/off, заморозка на X дней, отмена.
* История платежей, счета (PDF), поддержка/возврат (политика).
* Квитанции на e-mail.

---

## 9.10. Индивидуальный план (страница)

* **Статус:** ожидание анкеты → на согласовании → активен.
* Превью персональной недели; кнопка «Попросить замену блюда»; история изменений.
* Комментарии от нутрициолога (встроенный мини-чат).
* Повторная генерация по изменённым фильтрам.

---

## 9.11. Блог/Знания

* Статьи: «как готовить быстрее», «замены ингредиентов», «частые ошибки» и т.п.
* SEO-шаблоны, категории, поиск.

---

## 9.12. Поддержка/FAQ/Контакты

* База ответов: оплата, доступ, рецепты, аллергены, возвраты.
* Форма тикета (категория, вложение, автоответ).
* Политики и юридические страницы.

---

## 9.13. Кабинет рекламодателя

* Создание кампании: бюджет, даты, формат (баннер/HTML), таргет (разделы/страницы).
* Модерация (админ), оплата (тот же биллинг), статус.
* Статистика: показы/клики/расходы, выгрузка CSV.

---

## 9.14. Админ-панель (Filament)

* **Контент:** Меню/Дни/Рецепты/Ингредиенты (CRUD, массовый импорт), проверка ккал и веса порций.
* **Файлы месяца:** загрузка PDF/Excel, предпросмотр.
* **Планы/Подписки/Платежи:** списки, фильтры, карточки оплаты, рефанды, купоны.
* **Пользователи/Роли/Права:** действия, блокировки, журнал.
* **Реклама:** слоты/кампании/креативы, отчёты.
* **Настройки:** логотипы, письма, SEO, пиксели аналитики, метрики.
* **Отчёты:** выручка, MRR/Churn, активные подписки, LTV, топ-рецепты.

---

# 10) API/Интеграции (внутренние)

* **Платежные вебхуки** `/webhooks/yookassa`, `/webhooks/cloudpayments` → обработчик → очередь → запись `payments`/`user_subscriptions`.
* **Экспорт PDF/Excel** (задачи в очереди), CDN-ссылки в ЛК.
* **E-mail/уведомления:** подтверждение почты, успешная оплата, продление, окончание подписки, новый индивидуальный план, напоминания «список покупок на неделю готов».

---

# 11) Критерии готовности (acceptance)

* **Меню месяца** полностью доступно подписчику: для каждого дня 3 блюда, у каждого блюда **вес готовой порции** и **ккал** + **пошаговые инструкции**. &#x20;
* **PDF и Excel** доступны для скачивания в ЛК.
* **Список покупок** корректно суммирует по неделям и месяцам; экспорт.
* **Оформление подписки** (месяц/год/индивидуальный) и автопродление работают; статусы синхронизируются по вебхукам.
* **RBAC:** гость не видит платный контент; роли соблюдаются.
* **Админка:** контент/платежи/реклама управляются без разработчика.
* **Реклама:** показы/клики считаются, отчёты сходятся с логами.
* **Юр-страницы, почтовые шаблоны, SEO** — готовы.

---

# 12) Дорожная карта (MVP → расширение)

**MVP (спринты 1–3):**

* Регистрация/логин, планы, оплата месяц/год, ЛК (сегодня/неделя/файлы), каталог месяца, страница рецепта, списки покупок (без сложной логики хранения), админка контента, базовый биллинг.

**Этап 2:**

* Индивидуальные планы (анкета, генерация, чат), архив месяцев, реклама (минимум FIX/CPM), блог, купоны, аналитика.

**Этап 3:**

* Продвинутые рекомендации, интеграции (калькуляторы БЖУ, трекинг веса), реферальная программа, A/B-тесты, PWA/офлайн-лист покупок.

---

Если хочешь, превращу это ТЗ в **структурированный README/PRD** для репозитория (с чек-листами задач и схемой БД), а также добавлю **user flow диаграммы** и **макеты навигации**. Также могу подготовить список конкретных **миграций**/**моделей** и **роутов** (Laravel) под этот план.
