# Полный технический аудит проекта RawPlan
**Дата:** 13.02.2026  
**Версия:** Laravel 11.45.3 | PHP 8.3.25 | Filament 3.3.37

---

## 1. ОБЩАЯ ОЦЕНКА

| Категория | Оценка | Комментарий |
|-----------|--------|-------------|
| Архитектура | ✅ Хорошо | Чистая структура, разделение ответственности |
| Безопасность | ⚠️ Средне | Есть критические замечания |
| Производительность | ⚠️ Средне | N+1 проблемы, отсутствие индексов |
| Качество кода | ✅ Хорошо | Нет dd/dump, чистый код, хорошие комментарии |
| Тестирование | ✅ Хорошо | 25 тестов, покрытие ключевых модулей |
| Готовность к продакшину | ⚠️ Частично | Требуется устранение критических замечаний |

---

## 2. КРИТИЧЕСКИЕ ПРОБЛЕМЫ (🔴)

### 2.1. env() вызовы вне конфигов
**Файл:** `app/Http/Controllers/DiagnosticsController.php`  
**Строка 31:** `$this->secretKey = env('DIAGNOSTICS_KEY', '');`  
**Строки 412-413, 425-426:** `env('YOOKASSA_SHOP_ID')`, `env('CLOUDPAYMENTS_PUBLIC_ID')` и др.

**Проблема:** В продакшине после `config:cache` функция `env()` возвращает `null` вне файлов `config/*.php`. Диагностика перестанет работать, проверки платёжных систем будут ложно-отрицательными.

**Решение:** Все `env()` вызовы перенести в `config/` файлы и использовать `config()`.

### 2.2. YooKassa — placeholder ключи
**Файл:** `.env`  
```
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=your_secret
```
Платежи через YooKassa **не будут работать** с заглушками. Необходимо заменить на реальные ключи перед приёмом платежей.

### 2.3. Дублирование контроллеров планов
**Файлы:** `PlanController.php` и `PlansController.php`

Оба контроллера содержат дублирующуюся логику:
- `index()` — оба загружают планы
- `show()` — оба показывают детали плана
- `compare()` — оба реализуют сравнение
- `upgrade()` — оба реализуют апгрейд

Маршруты используют оба контроллера одновременно (web.php строки 44-49 и 87-89), что создаёт путаницу и потенциальные конфликты.

**Решение:** Объединить в один контроллер.

### 2.4. Дублирование обработки вебхуков
**Файлы:**
- `PaymentController.php` — методы `handleYooKassaWebhook()`, `handleCloudPaymentsWebhook()`, `updatePaymentStatus()`
- `Webhook/YooKassaWebhookController.php` — отдельный invokable контроллер
- `Webhook/CloudPaymentsWebhookController.php` — отдельный invokable контроллер

Маршруты в `web.php` указывают на Webhook-контроллеры, но `PaymentController` содержит свою реализацию `webhook()` метода (строка 128). Мёртвый код.

**Решение:** Удалить методы `webhook()`, `handleYooKassaWebhook()`, `handleCloudPaymentsWebhook()` из `PaymentController`.

### 2.5. Отсутствие ext-zip
Пакет `openspout/openspout` требует `ext-zip` для экспорта в Excel. Расширение **отсутствует**.

**Решение:** Установить `php8.3-zip` на сервере.

---

## 3. СЕРЬЁЗНЫЕ ПРОБЛЕМЫ (🟡)

### 3.1. N+1 проблемы в запросах

**DashboardController::shoppingList()** (строки 137-177):
- Цикл `getDayForDate()` выполняет отдельный запрос на каждый день (до 7 запросов)
- Внутри цикла `$day->load(['meals.recipe.ingredients'])` — ещё запросы на каждый день

**DashboardController::week()** (строки 76-96):
- Цикл из 7 итераций, каждая — отдельный запрос `getDayForDate()`
- `$day->meals()->count()` — ещё запрос на каждый день

**Menu::getShoppingList()** (строки 289-325):
- Тройной вложенный цикл по `days → meals → recipe.ingredients` без eager loading

**Recipe::isCompatibleWithUser()** (строки 259-283):
- `$this->ingredients()->count()` и `$this->ingredients` — два отдельных запроса

**Решение:** Использовать batch-загрузку дней за период одним запросом, eager loading для вложенных связей.

### 3.2. Статистика платежей загружает ВСЕ записи в память
**PaymentService::getPaymentStats()** (строка 146):
```php
$payments = $query->get(); // Загружает ВСЕ платежи в память
```
При росте данных это приведёт к OOM.

**Решение:** Использовать агрегатные SQL-запросы (`->selectRaw('COUNT(*), SUM(amount)...')`).

### 3.3. SubscriptionRoleService::syncAllUsersRoles() — загрузка всех пользователей
**Строка 186:** `$users = User::with(['subscriptions.plan'])->get();`

Загружает ВСЕХ пользователей с подписками в память. При росте базы — OOM.

**Решение:** Использовать `chunk()` или `cursor()`.

### 3.4. Роль 'root' в коде, но отсутствует в RBAC
**DashboardController::filterMenuBySubscription()** (строка 372):
```php
if ($user->hasRole('root')) { return; }
```
Роль `root` не определена в `SubscriptionRoleService` и не упоминается в сидерах/ожидаемых ролях DiagnosticsController (строка 472). Проверка никогда не сработает.

**Решение:** Заменить на `admin` или добавить роль `root` в систему.

### 3.5. DiagnosticsController проверяет pdo_mysql вместо pdo_pgsql
**Строка 134:** `$requiredExtensions = ['pdo', 'pdo_mysql', ...]`

Проект использует PostgreSQL (`DB_CONNECTION=pgsql`), но диагностика проверяет `pdo_mysql`.

**Решение:** Заменить `pdo_mysql` на `pdo_pgsql`.

### 3.6. DiagnosticsController::checkDatabase() вызывает `SELECT VERSION()`
**Строка 165:** `DB::selectOne('SELECT VERSION() as version')->version`

Это MySQL-специфичный синтаксис. В PostgreSQL работает, но возвращает строку вида `PostgreSQL 16.x`, а комментарий говорит "Версия MySQL".

**Решение:** Обновить текст и использовать `SHOW server_version` для PostgreSQL.

### 3.7. Отсутствие middleware `subscription` на маршрутах контента
Маршруты меню, рецептов, списка покупок (`/menus/*`, `/recipes/*`, `/shopping-list/*`) защищены только `auth`, но **не** middleware `subscription`. Любой авторизованный пользователь без подписки может получить доступ к контенту.

Middleware `CheckSubscription` зарегистрирован в `bootstrap/app.php`, но **нигде не используется** в маршрутах.

**Решение:** Добавить `subscription` middleware на маршруты контента.

### 3.8. Контактная форма отправляет письма на noreply@
**HomeController::contactSubmit()** (строка 64):
```php
Mail::to('noreply@rawplan.ru')->send(...)
```
Письма отправляются на адрес `noreply`, который обычно не читается.

**Решение:** Создать отдельный адрес (например, `support@rawplan.ru`).

### 3.9. Отсутствие канала логирования 'subscriptions' в .env
`SubscriptionLogService` использует `Log::channel('subscriptions')`, канал определён в `config/logging.php`, но `LOG_STACK` в `.env` = `daily`. Логи подписок пишутся в отдельный файл, но основной стек их не включает. Это нормально, но стоит убедиться, что ротация настроена.

---

## 4. ЗАМЕЧАНИЯ (🟢)

### 4.1. Архитектура и структура
- ✅ Чистая PSR-4 структура
- ✅ 20 моделей с правильными связями
- ✅ 7 сервисов с разделением ответственности
- ✅ 8 политик авторизации
- ✅ 4 кастомных middleware
- ✅ Event/Listener паттерн для подписок
- ✅ Отдельные каналы логирования (subscriptions, payments)
- ✅ Stubs для платёжных SDK (позволяют работать без реальных ключей)

### 4.2. Безопасность — что хорошо
- ✅ `APP_DEBUG=false` в продакшине
- ✅ `APP_ENV=production`
- ✅ HTTPS (`APP_URL=https://rawplan.ru`)
- ✅ Secure session cookies (`SESSION_SECURE_COOKIE=true`)
- ✅ Session encryption (`SESSION_ENCRYPT=true`)
- ✅ BCRYPT_ROUNDS=12
- ✅ Rate limiting на API (60/мин), auth (5/мин), contact (3/мин), webhooks (120/мин)
- ✅ CORS ограничен доменами rawplan.ru
- ✅ reCAPTCHA v3 на контактной форме
- ✅ HMAC-SHA256 верификация вебхуков YooKassa
- ✅ CSRF защита (вебхуки исключены корректно)
- ✅ Нет dd(), dump(), var_dump() в коде
- ✅ Нет TODO/FIXME/HACK в коде
- ✅ `env()` используется только в config/ файлах (кроме DiagnosticsController)
- ✅ Пароли хешируются через cast `hashed`
- ✅ `$hidden` на sensitive полях User

### 4.3. Модели — что хорошо
- ✅ Правильные `$fillable` (не `$guarded = []`)
- ✅ Правильные `casts()` для типов
- ✅ Scopes для частых запросов
- ✅ Бизнес-методы в моделях (BMI, статусы, проверки)
- ✅ Связи с type hints

### 4.4. Подписки и платежи — что хорошо
- ✅ Единая точка активации через `SubscriptionService`
- ✅ Защита от дублирования активации
- ✅ DB::beginTransaction() в критических операциях
- ✅ Grace period (3 дня) перед деактивацией
- ✅ Автопродление с запланированной сменой плана
- ✅ Логирование всех операций через `SubscriptionLogService`
- ✅ Роли синхронизируются через события
- ✅ Trial можно использовать только один раз
- ✅ Lapsed/Expired роли для бывших подписчиков

### 4.5. Планировщик — что хорошо
- ✅ 5 задач настроены с правильными интервалами
- ✅ `subscriptions:check-expired` — каждый час
- ✅ `subscriptions:process-renewals` — каждые 6 часов
- ✅ `subscriptions:sync-roles` — ежедневно в 3:00
- ✅ `subscriptions:retry-failed` — дважды в день
- ✅ `subscriptions:notify-expiring` — ежедневно в 10:00

### 4.6. Filament Admin — что хорошо
- ✅ 6 ресурсов (User, Plan, Menu, Recipe, Payment, UserSubscription)
- ✅ RelationManagers для вложенных данных
- ✅ Виджеты статистики (PaymentStats, SubscriptionStats, DashboardStats)
- ✅ Доступ ограничен ролями admin/editor через `canAccessPanel()`

### 4.7. Тесты
- ✅ 25 тестовых файлов
- ✅ Покрытие: публичные страницы, dashboard, меню, рецепты, подписки
- ✅ Тесты вебхуков (YooKassa, CloudPayments)
- ✅ Тесты сервисов (SubscriptionService, SubscriptionRoleService)
- ✅ Тесты политик (AdCampaignPolicy)
- ✅ Тесты Jetstream (auth, 2FA, API tokens)

### 4.8. Фронтенд
- ✅ Vite + TailwindCSS 3.4 + Alpine.js 3.15
- ✅ 19 директорий Blade-шаблонов
- ✅ Компоненты, layouts, emails, exports
- ✅ Build работает (`npm run build` — 3 файла, 185KB gzipped ~35KB)

---

## 5. РЕКОМЕНДАЦИИ ПО ПРИОРИТЕТУ

### Немедленно (перед запуском)

| # | Задача | Файл | Сложность |
|---|--------|------|-----------|
| 1 | Заменить YooKassa placeholder ключи | `.env` | Просто |
| 2 | Исправить `env()` в DiagnosticsController | `DiagnosticsController.php` | Просто |
| 3 | Добавить middleware `subscription` на маршруты контента | `web.php` | Средне |
| 4 | Установить `ext-zip` на сервере | Сервер | Просто |
| 5 | Исправить проверку `pdo_mysql` → `pdo_pgsql` в диагностике | `DiagnosticsController.php` | Просто |

### В ближайшее время

| # | Задача | Сложность |
|---|--------|-----------|
| 6 | Объединить PlanController и PlansController | Средне |
| 7 | Удалить мёртвый код webhook из PaymentController | Просто |
| 8 | Исправить N+1 в DashboardController (week, shoppingList) | Средне |
| 9 | Заменить `root` на `admin` в filterMenuBySubscription | Просто |
| 10 | Исправить адрес контактной формы | Просто |

### При масштабировании

| # | Задача | Сложность |
|---|--------|-----------|
| 11 | Оптимизировать PaymentService::getPaymentStats() — агрегатные запросы | Средне |
| 12 | Добавить chunk() в syncAllUsersRoles() | Просто |
| 13 | Оптимизировать Menu::getShoppingList() — eager loading | Средне |
| 14 | Добавить индексы БД для частых запросов (menus: month+year+is_published) | Средне |
| 15 | Перевести очереди на Redis (сейчас database) | Средне |

---

## 6. СТАТИСТИКА ПРОЕКТА

| Метрика | Значение |
|---------|----------|
| Модели | 20 |
| Контроллеры (Web) | 16 |
| Контроллеры (API) | 6 |
| Сервисы | 7 |
| Middleware | 4 кастомных |
| Политики | 8 |
| События | 2 |
| Слушатели | 3 |
| Консольные команды | 7 |
| Filament ресурсы | 6 |
| Тесты | 25 |
| Миграции | 32 (все выполнены) |
| Blade-директории | 19 |
| Маршруты Web | ~60 |
| Маршруты API | ~25 |
| Зависимости (composer) | 8 prod + 6 dev |
| Зависимости (npm) | 8 dev + 1 prod |
| Пользователи в БД | 8 |
| Активные сессии | 37 |

---

*Отчёт сгенерирован автоматически на основе анализа исходного кода проекта.*
