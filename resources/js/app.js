import './bootstrap';

// Alpine.js НЕ импортируем здесь:
// - На страницах с Livewire (app, guest) — Livewire 3 уже включает Alpine
// - На страницах без Livewire (public, landing) — Alpine подключается через CDN
